name: hdh_dbt
version: "1.0.0"
config-version: 2

profile: hdh_dbt

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  hdh_dbt:
    # Tất cả model build vào schema "analytics" (khai báo ở profiles.yml)
    # staging -> view, marts -> table (Iceberg)
    staging:
      +materialized: view
    marts:
      +materialized: table
